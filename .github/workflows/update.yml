name: Update Extensions

on:
    schedule:
        - cron: '0 */4 * * *'
    workflow_dispatch:
    push:
        branches:
            - main

permissions:
    contents: write

concurrency:
    group: ${{ github.workflow }}-${{ github.ref }}
    cancel-in-progress: false

jobs:
    update:
        runs-on: ubuntu-latest
        if: github.event_name != 'push' || github.event.head_commit.author.name != 'github-actions[bot]'
        outputs:
            updated: ${{ steps.update.outputs.updated }}

        steps:
            - name: Checkout repository
              uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

            - name: Install bun
              uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2.2.0
              with:
                  bun-version: latest

            - name: Install dependencies
              run: bun install --frozen-lockfile

            - name: Check extension updates
              id: update
              run: bun run update:check

            - name: Format extensions.json
              if: steps.update.outputs.updated == 'true'
              run: bun run format

            - name: Commit and push changes
              if: steps.update.outputs.updated == 'true'
              run: |
                  git config --local user.name "github-actions[bot]"
                  git config --local user.email "github-actions[bot]@users.noreply.github.com"
                  git add extensions.json
                  git diff-index --quiet HEAD || git commit -m "chore: update extensions.json"
                  git push

    sync-to-gitlab:
        runs-on: ubuntu-latest
        needs: update
        if: needs.update.outputs.updated == 'true' || github.event_name == 'workflow_dispatch'

        steps:
            - name: Checkout repository
              uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
              with:
                  ref: main
                  fetch-depth: 0

            - name: Sync to GitLab
              uses: pixta-dev/repository-mirroring-action@674e65a7d483ca28dafaacba0d07351bdcc8bd75 # v1.1.1
              with:
                  target_repo_url: ${{ secrets.GITLAB_REPO }}
                  ssh_private_key: ${{ secrets.GITLAB_SSH }}

    sync-to-dufs:
        runs-on: ubuntu-latest
        needs: update
        if: needs.update.outputs.updated == 'true' || github.event_name == 'workflow_dispatch'

        steps:
            - name: Check rclone config
              id: rclone
              run: |
                  if [ -n "$RCLONE_CONFIG" ]; then
                    echo "enabled=true" >> "$GITHUB_OUTPUT"
                  else
                    echo "enabled=false" >> "$GITHUB_OUTPUT"
                    echo "RCLONE_CONFIG is not set; skipping DUFS sync."
                  fi
              env:
                  RCLONE_CONFIG: ${{ secrets.RCLONE_CONFIG }}

            - name: Checkout repository
              if: steps.rclone.outputs.enabled == 'true'
              uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
              with:
                  ref: main

            - name: Setup Node.js
              if: steps.rclone.outputs.enabled == 'true'
              uses: actions/setup-node@v4
              with:
                  node-version: 22

            - name: Install bun
              if: steps.rclone.outputs.enabled == 'true'
              uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2.2.0
              with:
                  bun-version: latest

            - name: Install dependencies
              if: steps.rclone.outputs.enabled == 'true'
              run: bun install --frozen-lockfile

            - name: Prepare dist
              if: steps.rclone.outputs.enabled == 'true'
              run: bun run prepare:dist
              env:
                  S3_ENDPOINT: ${{ secrets.S3_ENDPOINT }}
                  S3_ACCESS_KEY_ID: ${{ secrets.S3_ACCESS_KEY_ID }}
                  S3_SECRET_ACCESS_KEY: ${{ secrets.S3_SECRET_ACCESS_KEY }}
                  S3_BUCKET_NAME: ${{ secrets.S3_BUCKET_NAME }}

            - name: Setup rclone
              if: steps.rclone.outputs.enabled == 'true'
              uses: AnimMouse/setup-rclone@e17fb0f092a6547544387f4ef0518a209fc69b0a # v1
              with:
                  rclone_config: ${{ secrets.RCLONE_CONFIG }}

            - name: Sync to DUFS
              if: steps.rclone.outputs.enabled == 'true'
              run: |
                  rclone sync ./dist dufs:/ \
                      --transfers 20 \
                      --checkers 20 \
                      --retries 3 \
                      --retry-sleep 5s \
                      --log-level INFO \
                      --progress
